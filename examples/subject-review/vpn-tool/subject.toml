[subject]
subject_id = "vpn-tool"
subject_type = "cli_workflow"
display_name = "VPN/proxy region tool"
description = "Cross-project helper for server connection failures, slow SSH, and regional proxy switching."
aliases = ["vpn", "proxy", "region", "netctl"]
owners = []
safety_tags = ["network", "cross_project"]

[context_window]
before_events = 2
after_events = 3
merge_distance_events = 2

[collectors.direct_usage]
tool_names = []
commands = ["netctl", "vpn-region-test", "proxy-test"]
skill_names = ["vpn-region-testing"]
mcp_names = []
subagent_names = []
